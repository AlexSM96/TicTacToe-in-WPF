<Window x:Class="TicTacToe.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:TicTacToe.ViewModels"
        xmlns:fa="http://schemas.fontawesome.com/icons/"
        xmlns:local="clr-namespace:TicTacToe"
        mc:Ignorable="d"
        WindowStyle="None"
        Height="550" Width="350"
ResizeMode="CanResizeWithGrip"
        Background="AntiqueWhite">
    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>
    <DockPanel>
        <UniformGrid DockPanel.Dock="Top" Rows="1" Columns="2" Margin="10,5,10,0">
            <Button Width="50" Opacity="1" HorizontalAlignment="Left" Click="RestartGame" IsDefault="True">
                <Button.Content>
                    <fa:ImageAwesome  Icon="Brands_Rev" Height="50" 
                                     Foreground="LightGreen">
                    </fa:ImageAwesome>
                </Button.Content>

            </Button>
            <fa:ImageAwesome Foreground="Red" Height="50" 
                             Icon="Regular_TimesCircle" 
                             HorizontalAlignment="Right">
                <fa:ImageAwesome.InputBindings>
                    <MouseBinding Command="{Binding CloseApplicationCommand}"
                                      MouseAction="LeftDoubleClick"/>
                </fa:ImageAwesome.InputBindings>
            </fa:ImageAwesome>
        </UniformGrid>
        <UniformGrid x:Name="UniformGridPanel" DockPanel.Dock="Top" Margin="0,15,0,0" 
              HorizontalAlignment="Center" Rows="3" Columns="3">

        </UniformGrid>
        <TextBlock Name="TextBlock" DockPanel.Dock="Bottom"
                   FontSize="38" FontWeight="Bold" HorizontalAlignment="Center">
        </TextBlock>
    </DockPanel>
</Window>
